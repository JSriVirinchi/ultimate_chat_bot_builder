<% botmessages_previous = "botmessages_previous_" + (@node.id).to_s %>
<% botresponses_messages_area_text_area = "botresponses_messages_area_text_area_" + (@node.id).to_s%>
document.getElementById('<%= botmessages_previous %>').innerHTML = "<%= j render 'home/edit/nodes/botmessages_previous', delete_button: true%>";
document.getElementById('<%= botresponses_messages_area_text_area%>').value = "";
